# Build directories
build/
cmake-build-*/

# OS-specific files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Project-specific IDE directories
.idea/
.vscode/

# Game-specific assets and binaries (Must NOT be uploaded)
bin/
original/
# Locally-staged deployment tree for testing on Vita3K/hardware -- same
# copyrighted game assets as bin/, just laid out under the on-device path
# (Data/, the three .so's, original.apk/.obb copies). Was accidentally
# tracked in Fase 4 (9894b9e); untracked going forward, see git history.
ux0_data/

# Compiled objects and artifacts
*.so
*.vpk
*.elf
*.self
eboot.bin

# Libc bridge build files
lib/libc_bridge/out/
lib/libc_bridge/out/*

# Temp or debug files
*.log
*.zip
*.obb
*.apk

# Sony firmware content (never redistribute)
*.suprx
logs
log_*.txt
apk_decompiled
build*
so_*/